/* ./__create/index.ts - placeholder
   Minimal server entry placeholder so the react-router dev plugin can resolve an entry.
   If you have a real server entry file, point vite.config.ts serverEntryPoint to that file instead.
*/
export {};
