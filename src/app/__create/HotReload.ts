// src/app/__create/HotReload.ts
// No-op hot reload helper for production builds.
// Dev server can replace/override this locally if needed.

export function initHotReload(): void {
  // intentionally empty in production
}

export default initHotReload;
